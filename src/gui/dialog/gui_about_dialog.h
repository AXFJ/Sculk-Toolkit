#pragma once

namespace gui_view {

// Floating "关于" window: program version and author. Drawn outside the main
// panel like the other dialogs.
void draw_about_window(bool& open, float dpi_scale);

} // namespace gui_view
